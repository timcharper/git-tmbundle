<table border=0 id="status_output">
<% status_data.each do |e| %>
  <tr>
    <td style='padding:0px 10px;'>
      <span title="<%= htmlize(e[:status][:long]) %>">
        <%= htmlize(e[:status][:short]) %>
      </span>
    </td>
    <td>
      <% if e[:path][-1..-1]=="/" %>
        <%= htmlize(e[:display]) %>
      <% else %>
        <%= link_to_textmate e[:display], e[:path] %>
      <% end %>
    </td>
  </tr>
<% end %>
</table>